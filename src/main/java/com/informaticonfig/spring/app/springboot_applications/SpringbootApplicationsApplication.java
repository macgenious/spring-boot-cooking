package com.informaticonfig.spring.app.springboot_applications;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class SpringbootApplicationsApplication {

	// Controller: class that handles HTTP requests
	// Server: an element in the network that manages requests and answers

	public static void main(String[] args) {
		SpringApplication.run(SpringbootApplicationsApplication.class, args);
	}

}
